# OS / editor cruft
.DS_Store

# Local agent + tooling state (not for the public site)
.claude/

# Internal growth-ops content (drafts, outreach, strategy) — not deployed
content/

# dependencies (restore with `npm ci`)
node_modules/

# wrangler files
.wrangler
.dev.vars*
!.dev.vars.example
.env*
!.env.example
